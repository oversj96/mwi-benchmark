import MwiBenchmarkView from "../src/components/MwiBenchmarkView";

// Just to be extra technical, this comment is so I can make a
// commit with the generic "firstname_lastname-MM-YYYY" as well.
const App = () => {
  return <MwiBenchmarkView />; 
}; 

export default App;
